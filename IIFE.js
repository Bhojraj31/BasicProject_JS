// IIFE :- It is Js function that runs as soon as as it defined.

// Usecase-It is used to avoid polluting the global namespace, execute an asunc-await , etc.

(
    function iife(){
        console.log("Hello");
    }
)();

iife();